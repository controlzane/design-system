# Claude Guidance

Read these before editing:

1. `AGENTS.md` for the local execution contract.
2. `AGENT.md` for the full implementation phases and verification checklist.
3. `cf-design-system.md` for long-form token, visual, and component specs.

Required gates after changes:

```bash
npm run lint
npm run build
```

Keep `/ui-preview` visually complete and `/ui-docs` aligned with actual component APIs.
